# Dependencies
node_modules/

# Env files (never commit real secrets)
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Build output
dist/
build/

# Backend uploads (user files shouldn't live in git)
backend/uploads/*
!backend/uploads/.gitkeep

# Logs / OS junk
*.log
.DS_Store
